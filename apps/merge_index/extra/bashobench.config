{mode, max}.

{duration, 10}.

{concurrent, 5}.

{report_interval, 5}.

{driver, basho_bench_driver_merge_index}.

{key_generator, {uniform_int_bin, 500000}}.

{value_generator, {uniform_int_bin, 500000}}.

{operations, [{index, 50}]}.
%{operations, [{info, 1}, {stream, 1}]}.

{code_paths, ["deps/stats",
              "deps/ibrowse",   
             "/Users/rusty/Documents/Basho/Search/apps/merge_index"]}.
