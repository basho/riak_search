#!/bin/sh
# postrm script for riak
#
# see: dh_installdeb(1)


# summary of how this script can be called:
#        * <postrm> `remove'
#        * <postrm> `purge'
#        * <old-postrm> `upgrade' <new-version>
#        * <new-postrm> `failed-upgrade' <old-version>
#        * <new-postrm> `abort-install'
#        * <new-postrm> `abort-install' <old-version>
#        * <new-postrm> `abort-upgrade' <old-version>
#        * <disappearer's-postrm> `disappear' <overwriter>
#          <overwriter-version>
# for details, see http://www.debian.org/doc/debian-policy/ or
# the debian-policy package


case "$1" in
    purge)
        rm -f /etc/default/riaksearch
        if [ -d /var/lib/riaksearch ]; then
                rm -r /var/lib/riaksearch
        fi
        if [ -d /var/log/riaksearch ]; then
                rm -r /var/log/riaksearch
        fi
        if [ -d /var/run/riaksearch ]; then
                rm -r /var/run/riaksearch
        fi
        if [ -d /etc/riaksearch ]; then
                rm -r /etc/riaksearch
        fi
        if [ -e /usr/sbin/riaksearch ]; then
                rm /usr/sbin/riaksearch
        fi
        if [ -e /usr/sbin/riaksearch-admin ]; then
                rm /usr/sbin/riaksearch-admin
        fi
        if [ -e /usr/sbin/search-cmd ]; then
                rm /usr/sbin/search-cmd
        fi
        if [ -e /etc/init.d/riaksearch ]; then
                rm  /etc/init.d/riaksearch
        fi
        if getent passwd riak >/dev/null; then
                deluser riak
        fi
        if getent group riak >/dev/null; then
                delgroup riak
        fi
    ;;

    remove|upgrade|failed-upgrade|abort-install|abort-upgrade|disappear)
    ;;

    *)
        echo "postrm called with unknown argument \`$1'" >&2
        exit 1
    ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
